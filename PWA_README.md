# 📱 PWA "Спаси Еду!" - Документация

## ✅ Что реализовано

### 1. **Web App Manifest** (`public/manifest.json`)
- ✅ Название: "Спаси Еду!"
- ✅ Короткое имя: "Спаси Еду"
- ✅ Описание: "Продукты со скидкой от 30% - спасаем еду от выбрасывания"
- ✅ Режим отображения: `standalone` (как нативное приложение)
- ✅ Цветовая тема: `#2c5f2d` (зелёный)
- ✅ Ориентация: портретная
- ✅ Категории: food, shopping, lifestyle
- ✅ Иконки: 8 размеров (72px - 512px)
- ✅ Shortcuts (быстрые действия):
  - Карта магазинов
  - Мои продукты

### 2. **Service Worker** (`public/sw.js`)

#### Стратегии кеширования:

**API запросы (`/api/*`)** - Network First
- Всегда пытаемся получить свежие данные из сети
- Кеш используется только при отсутствии связи
- **Безопасно:** авторизация и данные всегда актуальны

**Изображения продуктов (`/uploads/*`)** - Stale While Revalidate
- Показываем из кеша моментально
- Обновляем в фоне
- **Быстро:** картинки загружаются мгновенно

**Yandex Maps** - Network First
- Карты всегда свежие из сети
- Кеш для офлайн режима

**Статические файлы (JS, CSS)** - Cache First
- Сначала проверяем кеш
- Сеть только если нет в кеше
- **Быстро:** моментальная загрузка приложения

#### Ограничения:
- ✅ Макс. размер кеша: 50MB
- ✅ Автоудаление файлов старше 7 дней
- ✅ Версионирование кеша: `spasi-edu-v1`

### 3. **Favicon & Icons**
Созданы из `/674885.png`:

```
public/
├── favicon.ico (16x16, 32x32, 48x48)
├── favicon-16x16.png
├── favicon-32x32.png
├── apple-touch-icon.png (180x180)
└── icon-{72,96,128,144,152,192,384,512}x{размер}.png
```

### 4. **PWA Meta Tags** (`index.html`)
```html
<!-- Theme color для адресной строки браузера -->
<meta name="theme-color" content="#2c5f2d" />

<!-- iOS PWA поддержка -->
<meta name="apple-mobile-web-app-capable" content="yes" />
<meta name="apple-mobile-web-app-title" content="Спаси Еду!" />

<!-- Web App Manifest -->
<link rel="manifest" href="/manifest.json" />
```

### 5. **Service Worker Registration** (`src/main.jsx`)
- ✅ Автоматическая регистрация при загрузке
- ✅ Проверка обновлений каждые 30 минут
- ✅ Автоочистка кеша раз в сутки
- ✅ Логирование в консоль

## 🚀 Как проверить PWA

### Локально (Development)

1. **Запустить frontend:**
   ```bash
   cd food-discount-platform/frontend
   npm run dev
   ```

2. **Открыть Chrome DevTools:**
   - Открыть http://localhost:3012
   - F12 → вкладка **Application**

3. **Проверить Manifest:**
   - Application → Manifest
   - Должно быть: "Спаси Еду!", зелёная тема, 8 иконок

4. **Проверить Service Worker:**
   - Application → Service Workers
   - Статус: "activated and is running"
   - Scope: http://localhost:3012/

5. **Проверить Cache Storage:**
   - Application → Cache Storage
   - Должно быть: `spasi-edu-v1-static`, `spasi-edu-v1-data`

6. **Lighthouse PWA Audit:**
   - F12 → Lighthouse → Progressive Web App
   - Нажать "Analyze page load"
   - **Цель: 90-100 баллов**

### Production (https://спаси-еду.рф)

1. **Открыть сайт в Chrome на Android**

2. **Появится окно "Установить приложение":**
   - После 2-3 визитов
   - Или через меню браузера: "Установить приложение"

3. **После установки:**
   - Иконка на главном экране
   - Запуск как отдельное приложение
   - Нет адресной строки браузера

### iOS (Safari)

1. **Открыть сайт в Safari на iPhone**

2. **Добавить на экран "Домой":**
   - Нажать кнопку "Поделиться" [↑]
   - Выбрать "На экран «Домой»"
   - Название: "Спаси Еду!"

3. **Запустить приложение:**
   - Иконка на главном экране
   - Полноэкранный режим
   - Статус-бар зелёного цвета

## 🧪 Тестирование функциональности

### 1. Офлайн режим

```bash
# В Chrome DevTools:
Application → Service Workers → ✅ Offline
```

**Что должно работать:**
- ✅ Просмотр главной страницы
- ✅ Просмотр ранее загруженных продуктов
- ✅ Просмотр кешированных изображений
- ❌ Новые API запросы (ожидается)

### 2. Обновление кеша

**При изменении версии в sw.js:**
```javascript
const CACHE_VERSION = 'spasi-edu-v2'; // Изменить версию
```

- Старый кеш автоматически удаляется
- Новые файлы кешируются
- Пользователь получает обновлённую версию

### 3. Shortcuts (быстрые действия)

**Android:**
- Долгое нажатие на иконку приложения
- Должны появиться:
  - 🗺️ Карта магазинов
  - 🛒 Мои продукты

## 📊 PWA Checklist

### Основные требования
- ✅ HTTPS (обязательно для production)
- ✅ Service Worker зарегистрирован
- ✅ Web App Manifest с валидными данными
- ✅ Иконки 192x192 и 512x512
- ✅ `display: standalone` или `fullscreen`
- ✅ `start_url` указан
- ✅ `name` или `short_name`
- ✅ Favicon

### Дополнительные фичи
- ✅ Theme color
- ✅ Apple Touch Icon
- ✅ Описание
- ✅ Категории
- ✅ Shortcuts
- ✅ Screenshots (для магазина приложений)
- ✅ Offline fallback

### Оптимизация
- ✅ Версионирование кеша
- ✅ Автоочистка старых файлов
- ✅ Лимит размера кеша (50MB)
- ✅ Network First для API (безопасность)
- ✅ Cache First для статики (скорость)

## 🔧 Настройка и конфигурация

### Изменить название PWA

**Файл:** `public/manifest.json`
```json
{
  "name": "Новое название",
  "short_name": "Короткое",
  ...
}
```

### Изменить цветовую тему

**Файл:** `public/manifest.json` + `index.html`
```json
{
  "theme_color": "#новый_цвет",
  "background_color": "#новый_цвет"
}
```

```html
<meta name="theme-color" content="#новый_цвет" />
```

### Изменить стратегию кеширования

**Файл:** `public/sw.js`

Найти нужный тип запроса и изменить стратегию:
```javascript
// Пример: изменить API на Cache First
if (url.pathname.startsWith('/api/')) {
  event.respondWith(cacheFirstStrategy(request)); // Было: networkFirstStrategy
}
```

### Увеличить/уменьшить размер кеша

**Файл:** `public/sw.js`
```javascript
const MAX_CACHE_SIZE = 100 * 1024 * 1024; // 100MB (было 50MB)
const MAX_CACHE_AGE = 14 * 24 * 60 * 60 * 1000; // 14 дней (было 7)
```

## 🚨 Устранение проблем

### Service Worker не регистрируется

**Причина:** HTTPS обязателен (кроме localhost)

**Решение:**
```bash
# Проверить в консоли браузера:
console.log('serviceWorker' in navigator); // Должно быть true
```

### Старый кеш не удаляется

**Решение:**
```javascript
// В консоли браузера:
caches.keys().then(keys => keys.forEach(key => caches.delete(key)));

// Перезагрузить страницу
location.reload();
```

### PWA не предлагает установку

**Причины:**
1. Приложение уже установлено
2. Недостаточно визитов (нужно 2-3)
3. Не все требования выполнены

**Проверка:**
```bash
# Chrome DevTools → Console:
window.addEventListener('beforeinstallprompt', (e) => {
  console.log('PWA готово к установке!');
});
```

### Lighthouse даёт низкую оценку

**Частые проблемы:**
- ❌ Нет HTTPS (только localhost)
- ❌ Manifest невалидный
- ❌ Иконки неправильного размера
- ❌ Service Worker не кеширует `start_url`

**Решение:** Открыть DevTools → Lighthouse → Посмотреть конкретные ошибки

## 📱 Установка на разные устройства

### Android (Chrome/Edge)

**Автоматическое окно:**
- Появится после 2-3 визитов
- Кнопка "Установить" внизу экрана

**Вручную:**
1. Меню браузера (⋮)
2. "Установить приложение"
3. Подтвердить

### iOS (Safari)

**Только вручную:**
1. Кнопка "Поделиться" [↑]
2. "На экран «Домой»"
3. Название автоматически подставится: "Спаси Еду!"

### Desktop (Chrome/Edge)

**Вручную:**
1. Адресная строка → иконка "Установить" [⊕]
2. Или меню → "Установить Спаси Еду!"

## 🎯 Следующие шаги

### Возможные улучшения:

1. **Web Push Notifications**
   - Уведомления о новых продуктах
   - Требует отдельного сервера push

2. **Background Sync**
   - Отложенная отправка данных при плохом интернете
   - Магазин может добавить продукт офлайн

3. **Install Prompt Button**
   - Кнопка "Установить приложение" в UI
   - Контроль над показом окна установки

4. **Share Target API**
   - Делиться продуктами В приложение (не ИЗ)
   - Требует настройки в manifest

5. **Periodic Background Sync**
   - Автообновление данных в фоне
   - Экспериментальная фича Chrome

## 📄 Лицензия

Проект "Спаси Еду!" - Food Discount Platform
PWA реализация: 2025-10-25
